import styled from 'styled-components';

const StyledRoutes = styled.div`
  margin: 0 2.5vw;
`;

const RouteNumber = styled.div`
  font-family: fantasy;
  text-align: center;
`;

const RouteChain = styled.div`
  display: flex;
  justify-content: center;
`;

export { StyledRoutes, RouteNumber, RouteChain };
