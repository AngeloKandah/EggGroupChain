import styled from 'styled-components';

const StyledSidebar = styled.div`
  display: block;
  align-items: center;
  position: sticky;
  top: 0;
  height: 100vh;
  align-self: start;
`;

export { StyledSidebar };
