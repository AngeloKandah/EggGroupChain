import styled from 'styled-components';

const StyledSidebar = styled.div`
  display: block;
  align-items: center;
  position: sticky;
  top: 2.5vh;
  align-self: start;
  margin-bottom: 2.5vh;
`;

export { StyledSidebar };
