import styled from 'styled-components';

const StyledFigure = styled.figure`
  margin: 0;
  padding: 0;
`;

const PokeImg = styled.img`
  max-height: 3rem;
  max-width: 3rem;
`;

export { StyledFigure, PokeImg };
