import styled from 'styled-components';

const ChainList = styled.ul`
  display: flex;
  flex-direction: row;
  padding-left: 0;
  padding-top: 0;
  flex-wrap: wrap;
  border-style: solid;
  border-color: black;
  align-items: center;
`;

export default ChainList;
