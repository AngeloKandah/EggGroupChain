import styled from 'styled-components';

const ChainList = styled.ul`
  display: flex;
  flex-direction: row;
  padding-left: 0;
  padding-top: 0;
  flex-wrap: wrap;
  align-items: center;
  border-style: double;
  border-color: black;
  border-radius: 1rem;
`;

export default ChainList;
